<div>
  <%= f.label :description %>
  <%= f.text_area :description, placeholder: "Description" %>
</div>

<div>
  <%= f.label :film_type %>
  <% # collection_select(method, collection, value_method, text_method, options = {}, html_options = {})%>
  <%= f.collection_select :film_type, Project.film_types, :last, :last%>
</div>

<div>
  <%= f.label :genre %>
  <%= f.collection_select :genre, Project.genres, :last, :last%>
</div>

<div>
  <%= f.label :story_structure %>
  <%= f.text_field :story_structure, placeholder: "3 Act Structure" %>
</div>

<div>
  <%= f.label :themes %>
  <%= f.text_field :themes, placeholder: "symbolism" %>
</div>

<div>
  <%= f.label :link %>
  <%= f.text_field :link, placeholder: "Project video, website, etc." %>
</div>
<%= form_for @user, :url => { :action => "update" } do |f| %>
    <%= f.label "Enter the amount you wish to have in your account" %>
    <%= f.number_field :wallet %>
    <%= f.submit %>
    <% end %>